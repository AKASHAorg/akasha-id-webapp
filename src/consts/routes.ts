const userIdParam = ':userid'

const landing = '/'
const signUp = '/sign-up/'
const signIn = `/sign-in/${userIdParam}/`
const profile = '/profile/'
const apps = '/apps/'

export { userIdParam, landing, signUp, signIn, profile, apps }
