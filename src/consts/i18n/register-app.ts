import { REGISTER_APP } from '../i18n-ns'

const registerAppNs = `${REGISTER_APP}:`

const pageTitle = `${registerAppNs}pageTitle`
const text = `${registerAppNs}text`
const warning = `${registerAppNs}warning`
const selectPersona = `${registerAppNs}selectPersona`
const newPersona = `${registerAppNs}newPersona`
const authorize = `${registerAppNs}authorize`
const appRequests = `${registerAppNs}appRequests`

export { pageTitle, text, warning, selectPersona, newPersona, authorize, appRequests }
