import { APP_DETAILS } from '../i18n-ns'

const appDetailsNs = `${APP_DETAILS}:`

const requestDate = `${appDetailsNs}requestDate`
const about = `${appDetailsNs}about`
const link = `${appDetailsNs}link`
const website = {
  label: `${appDetailsNs}website.label`,
  button: `${appDetailsNs}website.button`,
}

const viewPersona = `${appDetailsNs}viewPersona`

const attributesHeader = `${appDetailsNs}attributesHeader`
const sharingAttributes = `${appDetailsNs}sharingAttributes`

const removeAccess = {
  header: `${appDetailsNs}removeAccess.header`,
  subheader: `${appDetailsNs}removeAccess.subheader`,
  button: `${appDetailsNs}removeAccess.button`,
}

const unlinkPersona = {
  header: `${appDetailsNs}unlinkPersona.header`,
  text: `${appDetailsNs}unlinkPersona.text`,
  button: `${appDetailsNs}unlinkPersona.button`,
}

const saveChanges = `${appDetailsNs}saveChanges`

export {
  requestDate,
  about,
  link,
  website,
  viewPersona,
  attributesHeader,
  sharingAttributes,
  removeAccess,
  unlinkPersona,
  saveChanges,
}
