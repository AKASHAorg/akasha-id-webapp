import { SETTINGS } from '../i18n-ns'

const settingsNs = `${SETTINGS}:`

const account = `${settingsNs}account`
const faqs = `${settingsNs}faqs`
const supportedApps = `${settingsNs}supportedApps`
const privacyPolicy = `${settingsNs}privacyPolicy`
const signOut = `${settingsNs}signOut`
const version = `${settingsNs}version`
const credits = `${settingsNs}credits`

export { account, faqs, supportedApps, privacyPolicy, signOut, version, credits }
