import { LANDING } from '../i18n-ns'

const landingNs = `${LANDING}:`

const signInWith = `${landingNs}signInWith`
const noUsersHeader = `${landingNs}noUsers.header`
const noUsersText = `${landingNs}noUsers.text`
const createAccount = `${landingNs}createAccount`

export { signInWith, noUsersHeader, noUsersText, createAccount }
