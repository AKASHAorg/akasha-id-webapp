import styled from 'styled-components'

const H1 = styled.h1`
  color: ${props => props.theme.colors.dark};
  font-size: 20px;
  line-height: 24px;
  font-weight: normal;
  letter-spacing: -0.4px;
  margin: 0 0 8px 0;
`

export default H1
